# Default Assumptions

| Assumption ID | Stage Introduced | Assumption Statement | Reasonable Default Chosen | Risk If Wrong | Confirmation Owner | Revisit Trigger | Current Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ASM-001` | `00-intake` | `/Users/zyongzhu/Workbase/Know-share` is the canonical project root from now on. | Imported prior Know-share materials into this workspace and created orchestrator files here. | Future work could diverge from the old workspace if the user expected the old path to remain canonical. | user | User says to use a different project root. | needs-confirmation |
| `ASM-002` | `00-intake` | The immediate objective is decomposition, not implementation. | Produce staged artifacts before coding so later multi-agent work has clear contracts. | Slower short-term progress if the user wanted immediate scaffolding. | user | User asks to implement directly before specs. | active |
| `ASM-003` | `00-intake` | Existing UI images are design inputs, not final page contracts. | Normalize image-derived features into `FUNCTION_OUTLINE.md`, `IA_SPEC.md`, `PAGE_SPEC.md`, and `FRONTEND_SPEC.md` before implementation. | Missing a visual detail if the images are treated too loosely. | agent | Stage 07 UI import and stage 08 page spec. | active |
| `ASM-004` | `00-intake` | MVP should remain privacy-first and agent-first. | Keep raw content out of platform scope; focus on manifest registry, discovery, exchange request/record, feedback, trust, and agent helper. | Product scope may feel smaller than the richer UI exploration. | user | Product spec confirmation. | active |
| `ASM-005` | `00-intake` | Multi-agent parallelization should happen after stable module boundaries exist. | Delay parallel implementation until the function outline, domain model, page spec, and service contracts exist. | Parallel agents may start later, but with less rework. | agent | Module workflow and service contract stages. | active |
| `ASM-006` | `02-product-brainstorming` | V0.1 should prioritize a static registry plus generated/public website instead of a database-heavy app. | Keep V0.1 focused on public manifest discovery, module detail, privacy-safe submission preview, and docs-first Agent skills. | If wrong, later stack and data ownership decisions may need to shift earlier. | user | Product spec confirmation. | needs-confirmation |
| `ASM-007` | `02-product-brainstorming` | Real exchange lifecycle can be V0.2, with V0.1 limited to contact/request intent. | Avoid early state-machine and permission complexity while privacy boundaries are confirmed. | If exchange must be V0.1, product/backend specs need a larger first release. | user | Product spec confirmation. | needs-confirmation |
| `ASM-008` | `02-product-brainstorming` | Full admin UI can follow after a minimal review workflow is defined. | Keep public submission safe without building all operations tooling first. | If public submissions open without moderation, privacy and abuse risk rises. | user | Product spec confirmation. | needs-confirmation |
