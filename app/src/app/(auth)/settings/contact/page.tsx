import { SettingsContactView } from "@/components/account";

/*
  PAGE-063 设置·联系方式（/settings/contact）。核心隐私面：默认私密 + 披露策略 + 同意记录。
*/
export default function SettingsContactPage() {
  return <SettingsContactView />;
}
